<div className="page">
  <div className="list">
    <div className="lname card">
      <h1 className="ltitle">Priemier League</h1>
    </div>
    {storeMatches.map((match) => (
      <a href={match.matchviewUrl}>
        <td className="cell">
          <img className="league_card" id={match} alt="match" />
          <div className="info pinfo">
            <h5 className="title">{match.title}</h5>
            <div className="pdate">{match.date.slice(0, 10)}</div>
          </div>
        </td>
      </a>
    ))}
  </div>
</div>