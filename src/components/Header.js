/* eslint-disable no-unused-vars */
import { NavLink } from 'react-router-dom';

const Header = () => (
  <nav className="navbar">
    <div className="views">League/match views</div>
  </nav>
);

export default Header;
